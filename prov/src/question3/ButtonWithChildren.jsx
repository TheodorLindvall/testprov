import React from "react";

const Button = ({ title, onClick }) => {
  return (
    <button onClick={onClick}>
      {title}
    </button>
  
  );
};
<p>"klick på button"</p>

export default Button;
